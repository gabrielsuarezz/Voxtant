{
  "name": "voxtant",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "build:web": "npm run build --workspace=web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
